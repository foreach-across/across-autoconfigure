server:
  port: 8888

endpoints:
  health:
    sensitive: false
  metrics:
    sensitive: false
  beans:
    sensitive: false

management:
  security:
    enabled: false

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/props,file:./,file:${java.io.tmpdir}/ax-cloud-test-props/
server:
  port: 8001

book:
  api:
    url: http://localhost:8888

feign:
  hystrix:
    enabled: true

management:
  endpoint:
    health:
      show-details: always

spring:
  jmx:
    enabled: false

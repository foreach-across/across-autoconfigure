server:
  port: 8001

book:
  api:
    url: http://localhost:8888

feign:
  hystrix:
    enabled: true

management:
  endpoint:
    health:
      show-details: always
    hystrix:
      config:
        x: y
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  jmx:
    enabled: false
